{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "MultiEdit",
      "Write",
      "Grep",
      "Glob",
      "LS",
      "TodoRead",
      "TodoWrite"
    ],
    "deny": []
  },
  "teammateMode": "tool-using",
  "hooks": {
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "head -90 governance/CONSTITUTION.md && echo '---' && python3 -c \"import json; data=json.load(open('governance/audit/finding-registry.json')); pending=[e for e in data if e.get('cycle')==7 and e.get('execution_status')=='pending']; print(f'Pending audit findings: {len(pending)}' if pending else 'Pending audit findings: Registry empty')\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'hatch3r-dev: governance/ changes require Binding Pillar (P1-P6) alignment. src/adapters/ changes require D09 checklist. Run: npm test && npx tsc --noEmit'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'hatch3r-dev: Content artifacts require YAML frontmatter (id, type, description, tags) + Pillar alignment + duplication check. Governance files must respect lean thresholds (CONSTITUTION <=200, AUDIT <=600, domains 30-80 lines).'"
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'hatch3r-dev: Sub-agent inherits governance. 6 Binding Pillars (P1-P6) in governance/CONSTITUTION.md. All changes must serve at least one.'"
          }
        ]
      }
    ]
  }
}
